# EXP-20260905-04 剷走「爛地孭重磅 −4」；配備變更問題早有定論

- **日期**：2026-09-05
- **平台**：AU
- **假設**：(a) `weight_score` 嘅「爛地孭重磅 −4」方向係反嘅；
  (b) `health_score` 值唔值得接通。
- **搜索過嘅舊記錄**：**EXP-20260826-07**（配備變更，REJECT）、
  EXP-20260826-03（降班輕磅方向修正）、EXP-20260905-02、
  memory `au-feature-set-is-saturated`、`cohort-cells-are-not-term-evidence`、
  `au-only-half-the-leaves-score`
- **改到嘅檔案**：`au_racing_engine/engine_core.py`（`_weight_score`）

## 前提：兩個 leaf 都係零排名權重
```
class_weight: (('rating_score', 0.7),)      ← weight_score 唔喺入面
track:        (('track_score', 1.0),)
```
`weight_score`（2026-07-30 退出）同 `health_score`（由來冇入過）都**唔喺
`MATRIX_FORMULAS`**。所以下面兩件都係**敘述真確性**，唔係績效改動。

⚠️ 更正 EXP-20260905-02 §3 一句：嗰度寫「綜合分係數 −0.9618 相消」。
嗰個 −0.9618 係場內經驗回歸（同其他 leaf 嘅相關），**唔係引擎係數**。
正確講法係「權重係零」。

## (a) 爛地孭重磅 −4 —— 剷走

**⚠️ 呢個語料橫跨兩個引擎版本**，所以唔可以逐個分支拆全窗口：

| weight_score | n | 出現日期 |
|---:|---:|---|
| 63（+3） | 372 | 08-13 … **08-31 停** |
| 57（−3） | 566 | 08-13 … 09-04 |

同一個「降班配輕磅」分支 08-31 之前係 +3、之後係 −3（EXP-20260826-03 嘅方向修正
到 09-01 先喺語料反映）。乾淨子集只有四日，冇 power。

唯一**唔受嗰次修正影響**、可以全窗口量嘅係「爛地孭重磅 −4」
（7,851 匹有出賽，入位率按當場派彩位數／馬匹數校正）：

| | n | 超額 | 95% CI |
|---|---:|---:|---|
| 60（冇中任何分支） | 6,072 | +0.5pp | [−0.6, +1.7] |
| **56（中呢個分支）** | **671** | **+3.5pp** | **[+0.1, +6.9]** |

被罰嗰批跑贏基準 **3.0pp，CI 過零**。同上面兩次方向修正同一個根因：
**讓磅官係按能力派磅**，爛地嘅高磅馬一樣係嗰場較好嗰批。

⚠️ 唔係新發現 —— 引擎註釋寫住當時量到 +2.98pp [−0.40, +6.30]，判「唔夠顯著
所以唔郁」。今次獨立重量，同號同幅度、樣本更大、CI 收窄到過零，
所以由「未夠證據」變成「有證據，而且係反嘅」。

**判斷標準（合約第 7 節）**：就算績效數字係相反方向，報告都唔應該一路講
「體能消耗顯著」一路扣 4 分，而嗰批馬實際跑贏。→ 剷走扣分同嗰句敘述。

## (b) health_score —— 唔好接通去排名，早有定論

八個 input 有六個結構性空（`docs/audits/AU_TRACK_JHF_DATA_QUALITY_2026-09-05.md`）：
`warning_line` / `spell_days` 個 key 由來唔存在；`gear_line` / `gear_changes` /
`has_blinkers` / `timing_trial_600m_avg_speed` 100% 有 key、**0% 有值**。
只剩「距上仗日數」一條路活住 → 全語料得 59 / 60 / 61 三個值。

配備數據確實存在（存檔 Formguide 85.9% 有 `SportsbetGear:`，21.5% runner 有變更
文字），**但 EXP-20260826-07 已經測過並 REJECT**：

- 訊號真：除下配備 −3.86pp [−6.94, −0.75] 顯著
- **但同 `form_score` 重複**：有配備變更嘅馬 form_score 平均 59.83 vs 62.10，差 2.27 分
- 四個 k 值（0.5 / 1 / 2 / 3）**冇一個過閘**

→ 唔好為咗「令 health_score 有內容」而重做一次已經 REJECT 咗嘅嘢。
EXP-20260826-07 自己嘅建議係「**值得抽落嚟出報告**」，嗰件仲未做。

## 檢查
- **leakage-audit**：PASS（只用賽前欄位）
- **golden_scoring**：AU 120/120 一致（`weight_score` 零權重，排名 bit-identical）
- **run_tests**：AU 引擎 621 個測試全綠
- **退步**：冇

## 結論
1. 剷走「爛地孭重磅 −4」。零排名影響，純敘述真確性（合約第 7 節）。
   ⚠️ 明文聲明：**呢個唔係已證實嘅績效改善。**
2. `health_score` 唔好接落排名 —— 佢最有機會嘅內容（配備變更）2026-08-26 已經
   REJECT，而且直接量到同 form 重複。**剩返嘅選擇係報告層，唔係排名層。**
3. 「呢個窗口逐個分支拆 `weight_score`」呢個做法本身唔成立 —— 08-31 有個
   engine 版本界線。任何跨 08-31 嘅 `weight_score` cohort 分析都要重做。
